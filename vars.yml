---
project_name: "caktus"
env_name: "local"
domain: "caktusgroup.com"

db_password: "password"

users:
  - { name: 'vagrant', public_key: [] }

app_minions: "{{ groups['web'] | union(groups['worker']) }}"

python_version: 2.7
